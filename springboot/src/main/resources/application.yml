server:
  port: 9090

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springboot_vue2022?serverTimezone=GMT%2b8
    username: root
    password: root

#mybatis:
#  mapper-locations: classpath:mapper/*.xml #找寻对应生成的mapper.xml 的路径
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #Mybatis的sql日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #Mybatis-plus的sql日志
  mapper-locations: classpath:mapper/*.xml #找寻对应生成的mapper.xml 的路径
